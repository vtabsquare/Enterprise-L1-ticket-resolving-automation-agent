/**
 * components/index.js — Shared UI components barrel export.
 * Components will be added progressively through Phase 6.
 *
 * Planned components:
 *   - StatusBadge   — coloured badge for ticket/action status
 *   - PriorityBadge — coloured badge for ticket priority
 *   - Pagination    — page controls for tables
 *   - Spinner       — loading indicator
 *   - ErrorBanner   — API error display
 *   - Timeline      — agent action timeline for TicketDetail
 */

// Placeholder — components added in Phase 6
export {}
